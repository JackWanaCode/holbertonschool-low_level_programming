#include <stdio.h>
#include "holberton.h"

/**
 * print_numbers - Entry point
 * Description: use _putchar to print 0 to 9 to screen..
 *
 * Return: nothing.
 */
void print_numbers(void)
{
	char i = '0';

	while (i <= '9')
	{
		_putchar(i);
		i++;
	}
	_putchar('\n');
}
