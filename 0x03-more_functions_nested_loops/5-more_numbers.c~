#include <stdio.h>
#include "holberton.h"

/**
 * _isupper - Entry point
 * Description: check Upper Case for a letter.
 * @c: variable c is to be checked.
 *
 * Return: 1 for true, 0 for false.
 */
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
		return (1);
	return (0);
}
